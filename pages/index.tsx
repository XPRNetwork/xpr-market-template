import Head from 'next/head';
import { FC } from 'react';
import { Header } from '../components';

const HomePage: FC = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default HomePage;
